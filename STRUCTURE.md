# Структура проекта LogiCalc

Этот документ описывает архитектуру и организацию файлов в проекте LogiCalc.

## 📁 Структура папок

```
LogiCalc/
├── public/                     # Статические файлы
│   ├── icons/                  # Иконки приложения
│   ├── images/                 # Изображения
│   └── favicon.ico            # Иконка сайта
│
├── src/                       # Исходный код приложения
│   ├── app/                   # Next.js App Router
│   │   ├── (auth)/           # Группа маршрутов аутентификации
│   │   ├── (dashboard)/      # Группа маршрутов дашборда
│   │   ├── api/              # API маршруты
│   │   ├── globals.css       # Глобальные стили
│   │   ├── layout.tsx        # Корневой layout
│   │   └── page.tsx          # Главная страница
│   │
│   ├── components/           # React компоненты
│   │   ├── ui/              # Базовые UI компоненты
│   │   ├── auth/            # Компоненты аутентификации
│   │   ├── dashboard/       # Компоненты дашборда
│   │   ├── analytics/       # Компоненты аналитики
│   │   └── marketplace/     # Компоненты маркетплейсов
│   │
│   ├── hooks/               # Custom React hooks
│   │   ├── useAuth.ts       # Хук аутентификации
│   │   ├── useSupabase.ts   # Хук для работы с Supabase
│   │   └── useLocalStorage.ts # Хук для работы с localStorage
│   │
│   ├── lib/                 # Библиотеки и утилиты
│   │   ├── supabase.ts      # Конфигурация Supabase
│   │   ├── constants.ts     # Константы приложения
│   │   └── validations.ts   # Схемы валидации
│   │
│   ├── store/               # Управление состоянием (Zustand)
│   │   ├── app.ts           # Глобальное состояние приложения
│   │   ├── marketplace.ts   # Состояние маркетплейсов
│   │   └── analytics.ts     # Состояние аналитики
│   │
│   ├── types/               # TypeScript типы
│   │   ├── database.ts      # Типы базы данных
│   │   ├── api.ts           # Типы API
│   │   └── marketplace.ts   # Типы маркетплейсов
│   │
│   ├── utils/               # Вспомогательные функции
│   │   ├── index.ts         # Основные утилиты
│   │   ├── format.ts        # Функции форматирования
│   │   └── validation.ts    # Функции валидации
│   │
│   ├── api/                 # API клиенты
│   │   ├── ozon.ts          # Клиент Ozon API
│   │   ├── wildberries.ts   # Клиент Wildberries API
│   │   └── yandex-market.ts # Клиент Яндекс.Маркет API
│   │
│   └── middleware.ts        # Next.js middleware
│
├── supabase/                # Конфигурация Supabase
│   ├── migrations/          # SQL миграции
│   └── seed.sql            # Тестовые данные
│
├── .env.local              # Переменные окружения (локальные)
├── .env.example            # Пример переменных окружения
├── package.json            # Зависимости проекта
├── tsconfig.json           # Конфигурация TypeScript
├── tailwind.config.js      # Конфигурация Tailwind CSS
├── next.config.js          # Конфигурация Next.js
└── README.md               # Документация проекта
```

## 🏗️ Архитектурные принципы

### 1. Разделение ответственности
- **`components/`** - Переиспользуемые UI компоненты
- **`pages/`** - Страницы приложения (Next.js App Router)
- **`hooks/`** - Бизнес-логика в виде custom hooks
- **`store/`** - Глобальное состояние приложения
- **`utils/`** - Чистые функции без побочных эффектов

### 2. Типизация
- Все компоненты и функции строго типизированы
- Типы базы данных автогенерируются из Supabase
- Отдельные файлы типов для каждого домена

### 3. Модульность
- Каждый модуль имеет свою папку
- Компоненты сгруппированы по функциональности
- Общие компоненты в папке `ui/`

## 🔧 Соглашения по коду

### Именование файлов
- **Компоненты**: PascalCase (`ProductCard.tsx`)
- **Hooks**: camelCase с префиксом `use` (`useAuth.ts`)
- **Утилиты**: camelCase (`formatCurrency.ts`)
- **Типы**: camelCase (`userTypes.ts`)

### Экспорт/Импорт
- Именованный экспорт для компонентов
- Дефолтный экспорт для страниц
- Barrel exports в файлах `index.ts`

### Стилизация
- Tailwind CSS для всех стилей
- Компоненты UI из Shadcn/ui
- CSS модули только при необходимости

## 📦 Управление состоянием

### Zustand Stores
- **`app.ts`** - Глобальное состояние (пользователь, тема, уведомления)
- **`marketplace.ts`** - Данные маркетплейсов и продуктов
- **`analytics.ts`** - Аналитические данные и метрики

### Локальное состояние
- React hooks (`useState`, `useReducer`) для компонент-специфичного состояния
- Custom hooks для повторяющейся логики

## 🔐 Безопасность

### Аутентификация
- Supabase Auth для управления пользователями
- Middleware для защиты маршрутов
- Row Level Security (RLS) в базе данных

### API безопасность
- Валидация всех входящих данных
- Защищенные эндпоинты через middleware
- Шифрование чувствительных данных

## 🚀 Производительность

### Оптимизация загрузки
- Code splitting через dynamic imports
- Lazy loading компонентов
- Оптимизация изображений через Next.js Image

### Кэширование
- React Query для кэширования API запросов
- Zustand persist для сохранения состояния
- Service Worker для офлайн функциональности

## 📱 Адаптивность

### Responsive Design
- Mobile-first подход
- Breakpoints: sm (640px), md (768px), lg (1024px), xl (1280px)
- Адаптивные компоненты с Tailwind CSS

### Доступность (A11y)
- Семантическая разметка
- ARIA атрибуты
- Поддержка клавиатурной навигации
- Контрастность цветов

## 🧪 Тестирование

### Типы тестов
- **Unit tests** - Jest + React Testing Library
- **Integration tests** - Cypress
- **E2E tests** - Playwright

### Покрытие кода
- Минимум 80% покрытие для критических компонентов
- Автоматическое тестирование в CI/CD

## 🔧 Разработка

### Команды разработки
```bash
npm run dev          # Запуск development сервера
npm run build        # Сборка для production
npm run start        # Запуск production сервера
npm run lint         # Проверка линтером
npm run type-check   # Проверка типов TypeScript
```

### Git workflow
- Feature branches для новых функций
- Pull requests для review кода
- Conventional commits для сообщений

Этот документ будет обновляться по мере развития проекта.